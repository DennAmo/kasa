import React from "react";
import LogementPageDisplay from "../components/LogementPageDisplay/LogementPageDisplay";

const LogementPage = () => {
  return (
    <main>
      <LogementPageDisplay />
    </main>
  );
};

export default LogementPage;
